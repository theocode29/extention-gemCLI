description = "Run BONE semantic lint. Usage: /dp:lint <path>"
prompt = """
Run semantic lint for a file or directory.

Input: {{args}}

Requirements:
- Path is required.
- If missing, return exactly: ERROR: missing required argument <path>

Execution:
- Call MCP tool bone_semantic_lint with:
  {"path":"<path>"}

Return pass/fail and actionable errors.
"""
