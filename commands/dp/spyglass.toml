description = "Run Spyglass syntax validation. Usage: /dp:spyglass <path>"
prompt = """
Run Spyglass validation.

Input: {{args}}

Requirements:
- Path is required.
- If missing, return exactly: ERROR: missing required argument <path>

Execution:
- Call MCP tool run_spyglass_cli with:
  {"path":"<path>"}

Return pass/fail and errors.
"""
