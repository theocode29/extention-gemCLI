description = "Run dynamic test. Usage: /dp:test [gametest|bookshelf_status]"
prompt = """
Run headless test.

Input: {{args}}

Rules:
- Allowed values: gametest, bookshelf_status.
- If empty, default to gametest.
- If value is not allowed, return exactly:
  ERROR: invalid value for <type>. Allowed: gametest|bookshelf_status

Execution:
- Call MCP tool run_headless_test with:
  {"type":"<resolved_type>"}

Return test outcome.
"""
