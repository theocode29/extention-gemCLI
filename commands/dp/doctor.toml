description = "Run project diagnostic workflow (deps, manual mods, assets, sync)."
prompt = """
Run a diagnostics bundle and output a single report.

Execution sequence:
1) Call fs_verify_dependencies.
2) Call get_manual_modifications.
3) Call fs_verify_assets.
4) Call fs_sync_all.

Output format:
- Dependencies
- Manual Modifications
- Assets
- Sync Status
- Recommended Next Actions

If one step fails, continue with remaining steps and mark failed section explicitly.
"""
